// <auto-generated>
//     This code was generated with love by Gentitas.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

using Entitas;
using System.Collections.Generic;

namespace Gentitas.Lib {
    public static class KernelService {
        #if UNITY_EDITOR
        static Entitas.VisualDebugging.Unity.DebugSystems kernels;
        #else
        static Systems kernels;
        #endif

        public static void Initialize(KernelName kernel) {
            UnityEngine.Debug.Log("Chosen kernel: " + kernel);

            {{#if meta.choosableKernels}}
            switch (kernel) {
                {{#each meta.choosableKernels}}
                {{#each this}}
                    case KernelName.{{moduleNameMergedValue}}{{nameUpperValue}}:
                        kernels = new {{moduleNameValue}}.Kernels.{{classNameValue}}();
                        break;
                {{/each}}
                {{/each}}
            }
            {{/if}}

            kernels.Initialize();
        }

        public static void Execute() {
            kernels.Execute();
        }
    }

    public enum KernelName {
    {{#each meta.choosableKernels}}
        {{#each this}}{{moduleNameMergedValue}}{{nameUpperValue}}{{#unless @last}}, {{/unless}}{{/each}}{{#unless @last}}, {{/unless}}
    {{/each}}
    }
}
