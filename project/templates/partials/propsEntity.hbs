{{#each propsEntity}}
protected bool {{context.name}}{{#if ucomponent.prefix}}{{toUpper ucomponent.prefix}}{{else}}Has{{/if}}{{toUpper name}}{{toUpper ucomponent.name}} {
    get { return Contexts.sharedInstance.{{context.name}}.{{#if ucomponent.prefix}}{{toLower ucomponent.prefix}}{{else}}has{{/if}}{{toUpper ucomponent.name}}; } }

protected {{context.nameUpper}}Entity {{context.name}}{{toUpper ucomponent.name}}Entity {
    get { return Contexts.sharedInstance.{{context.name}}.{{toLower ucomponent.name}}Entity; } }

{{#if ucomponent.fields}} 
protected {{toUpper ucomponent.name}}Component {{context.name}}{{toUpper ucomponent.name}} {
    get { if ({{context.name}}{{toUpper ucomponent.name}}Entity == null) return null;
          return {{context.name}}{{toUpper ucomponent.name}}Entity.{{toLower ucomponent.name}}; } }
          
{{/if}}
{{/each}}